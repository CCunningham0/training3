// standard function syntax
function hello(name) {
    console.log('Hello ' + name);
}
hello('Mark');

// arrow function syntax
const result = (name) => 'Hello ' + name; // result holds a function
console.log(result('John'));